name: Update Dojo

on: push

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: pwncollege/dojo-update@v1
        with:
          dojo: welcome~8598b4dd
          update_code: ${{ secrets.UPDATE_CODE }}
